firstTextToConcat = prompt("Enter the first text to be concat");
secondTextToConcat = prompt("Enter the second text to be concat");
concatenatedText = firstTextToConcat + secondTextToConcat;
alert(`The concatenated text is ${concatenatedText}`);
